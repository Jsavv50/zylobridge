# My Work Visual QA

Desktop captures were taken for `/my-work` and `/my-work/1` at 1280×720. Both routes loaded the shared ZYLOBRIDGE navigation and displayed their initial skeleton states without horizontal overflow. The command center skeleton reserves the intended hero, summary-card, and work-section hierarchy; the workspace skeleton reserves breadcrumb, hero, and detail-section hierarchy. Authenticated data rendering requires a professional session with an engagement record, so the desktop capture intentionally does not claim populated production work data.

## Mobile QA

Narrow mobile captures were taken for `/my-work` and `/my-work/1` at 390×844. The shared header collapses to the branded logo and menu control; page skeletons stack vertically with safe horizontal padding, and no horizontal overflow was visible. The layout keeps the primary content blocks within the viewport and is ready for populated professional-session verification.
